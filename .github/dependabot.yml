version: 2
updates:
  # Keeps the SHA-pinned actions in .github/workflows current: Dependabot
  # opens PRs bumping the pins so they don't silently rot.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
